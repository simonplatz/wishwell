import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

export default Login => {
    return (
        <View>
           <Text>hello fucking joakim din skid</Text> 
        </View>
    )
};

const style = StyleSheet.create({
    container: {
      width: "100%",
      padding: 4,
    }
});
